version: '3.8'

services:
  mongodb:
    image: mongo
    container_name: ThoughtCache-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=ThoughtCache
    networks:
      - ThoughtCache-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: ThoughtCache-backend
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - MONGODB_URI=mongodb://mongodb:27017/ThoughtCache
      - JWT_SECRET=${JWT_SECRET}
      - PORT=3000
      - JWT_EXPIRES_IN=7d
      - CORS_ORIGIN=http://localhost
      - RAG_SERVICE_URL=http://rag:8000
    depends_on:
      - mongodb
      - rag
    networks:
      - ThoughtCache-network

  rag:
    build:
      context: ./rag
      dockerfile: Dockerfile
    container_name: ThoughtCache-rag
    restart: unless-stopped
    env_file:
      - ./rag/.env
    environment:
      - PORT=8000
    networks:
      - ThoughtCache-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: ThoughtCache-frontend
    restart: unless-stopped
    ports:
      - "80:80"
    environment:
      - VITE_API_BASE_URL=/api
    depends_on:
      - backend
    networks:
      - ThoughtCache-network

volumes:
  mongo-data:
    driver: local

networks:
  ThoughtCache-network:
    driver: bridge
